Dropbox-mnt is a work in progress. My end goal is to have an efficient filesystem which uses caching and goroutines to mount my dropbox account.

==WORKING FEATURES==
cd and ls... its a start!

==INSTRUCTION==
go install
./dropbox-mnt MOUNTPATH
